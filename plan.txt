databases and collections

keep raw pages on disk as .json

# indexed by date and comment_id (string, stem, and lemma data)

db.askreddit.comments

for each reddit page:
posting title is in page[0] (level 0)
comments are organized hierarchically in page[1]['data']['children'] (level 1)
replies are contained in page[1]['data']['children'][i]['data']['replies']['data']['children'] (level 3)

# count data indexed by date, token, length (also include ids)

db.askreddit.stringsCount
db.askreddit.stemsCount
db.askreddit.lemmasCount
db.askreddit.stringsActivation
db.askreddit.stemsActivation
db.askreddit.lemmasActivation

# map data indexed by token, length, and type

db.askreddit.positionMap
db.askreddit.commentMap
db.askreddit.parentMap
db.askreddit.childrenMap

functions and output

track_token
track_string
fetch_context
sentiment?
return top i ngrams
build count corpora
build activation corpora
map position
map comment
map parent
map children
zipf test
shannon test
